import type { NavigateFunction } from "react-router-dom";
import { paths } from "./appRoutes";

const SECTIONS = [
  { id: "intro", label: "Введение" },
  { id: "concepts", label: "Основы VPN и панели" },
  { id: "interface", label: "Интерфейс" },
  { id: "servers", label: "Серверы и узлы" },
  { id: "working-with-node", label: "Работа с узлом" },
  { id: "dns-firewall", label: "DNS и межсетевой экран" },
  { id: "users-organizations", label: "Пользователи и организации" },
  { id: "logs-tasks", label: "Журнал и задачи" },
  { id: "panel-settings", label: "Настройки панели" },
  { id: "agent-security", label: "Агент и безопасность" },
  { id: "troubleshooting", label: "Проблемы и советы" },
];

function DocBody({ section, navigate }) {
  switch (section) {
    case "intro":
      return (
        <>
          <h2>Введение</h2>
          <p className="doc-lead">
            Эта инструкция рассчитана на администратора, который <strong>раньше не работал</strong> с этой панелью, с{" "}
            <strong>OpenVPN</strong>, с <strong>DNSMasq</strong> и с настройкой <strong>межсетевых экранов</strong> на
            Linux. Термины по ходу объясняются; от вас ожидается общее понимание, что такое «сервер», «IP-адрес» и
            «вход в систему по логину и паролю».
          </p>

          <h3>Что такое OVPN Control в одном абзаце</h3>
          <p>
            <strong>OVPN Control</strong> — это веб-приложение (панель), через которое несколько администраторов могут{" "}
            <strong>учитывать</strong> VPN-серверы на ваших машинах, <strong>смотреть</strong> нагрузку и сессии,{" "}
            <strong>менять настройки</strong> OpenVPN и сопутствующих служб на узле, <strong>вести</strong> список
            пользователей и организаций и <strong>запускать</strong> длительные операции через очередь задач. Панель сама
            по себе <strong>не заменяет</strong> установку OpenVPN на сервере: на каждом узле должен быть установлен и
            настроен <strong>агент</strong> — небольшая программа, с которой панель общается по сети по защищённому
            каналу (HTTP или HTTPS) и секретному <strong>токену</strong>.
          </p>

          <h3>Основные возможности панели</h3>
          <ul className="doc-list">
            <li>
              <strong>Учёт серверов (узлов)</strong> — список машин с агентом, адрес API, метрики (CPU, RAM, сеть),
              число активных VPN-подключений.
            </li>
            <li>
              <strong>Карточка сервера</strong> — вкладки: обзор службы OpenVPN, список системных служб (в т.ч.
              перезапуск), DNS (dnsmasq), мониторинг, активные сессии, сертификаты и ключи на узле, параметры связи с
              агентом, настройки OpenVPN и клиента, межсетевой экран на узле, журнал.
            </li>
            <li>
              <strong>Организации и пользователи VPN</strong> — справочник организаций, карточки пользователей, сессии,
              сертификаты, персональные настройки туннеля (CCD), при необходимости правила firewall на уровне
              пользователя и организации.
            </li>
            <li>
              <strong>Журнал</strong> — события панели: назначение VPN-адресов, исходные IP, действия администраторов.
            </li>
            <li>
              <strong>Задачи</strong> — очередь фоновых операций (применение конфигураций, DNS и т.д.) с возможностью
              повторить после ошибки.
            </li>
            <li>
              <strong>Настройки</strong> — администраторы панели, приглашения, блокировка, резервное копирование и
              восстановление данных панели.
            </li>
          </ul>

          <h3>Особенности работы</h3>
          <ul className="doc-list">
            <li>
              Доступ только после <strong>входа</strong>; сессия ограничена по времени (см. таймер вверху справа).
            </li>
            <li>
              Опасные действия (остановка служб, перезапуск агента, отзыв доступа и т.п.) требуют{" "}
              <strong>подтверждения</strong> в модальном окне.
            </li>
            <li>
              Панель и агент должны <strong>достигать друг друга по сети</strong> (хост, порт, при необходимости
              открытые порты на межсетевом экране <em>между</em> панелью и узлом).
            </li>
            <li>
              Изменения на узле выполняет <strong>агент</strong> от имени системной учётной записи на сервере; у этой
              учётной записи должны быть права на те команды, которые панель запрашивает (например{" "}
              <code>systemctl</code> для указанных служб).
            </li>
          </ul>

          <div className="doc-callout">
            Документация описывает типичное поведение интерфейса. Точный набор вкладок и полей может слегка отличаться
            в вашей сборке панели — ориентируйтесь на подписи в интерфейсе и на разделы ниже как на логическую модель.
          </div>
        </>
      );

    case "concepts":
      return (
        <>
          <h2>Основы: VPN, OpenVPN и как устроена панель</h2>

          <h3>Что такое VPN (очень кратко)</h3>
          <p>
            <strong>VPN</strong> (виртуальная частная сеть) в корпоративном контексте чаще всего означает: сотрудник
            (или устройство) устанавливает на своём компьютере программу-клиент и получает зашифрованный «туннель» до
            вашего сервера. После подключения клиент может получать доступ к внутренним адресам и сервисам так, как
            будто он находится в вашей сети. В этом проекте роль сервера туннеля выполняет <strong>OpenVPN</strong> на
            Linux-машине, а панель помогает этим серверам управлять централизованно.
          </p>

          <h3>Что такое OpenVPN</h3>
          <p>
            <strong>OpenVPN</strong> — это конкретная реализация VPN-сервера и клиента с открытым исходным кодом. На
            сервере работает процесс, который слушает подключения (обычно по UDP или TCP), проверяет{" "}
            <strong>сертификаты</strong> или другие методы аутентификации и маршрутизирует трафик клиентов. Конфигурация
            задаётся файлами (часто <code>server.conf</code> на сервере и профиль на клиенте).{" "}
            <strong>OVPN Control не заменяет</strong> чтение официальной документации OpenVPN для глубокой настройки, но
            даёт формы и кнопки для типовых операций в вашей организации.
          </p>

          <h3>Сертификаты «простыми словами»</h3>
          <p>
            Для OpenVPN часто используются <strong>криптографические сертификаты</strong> (файлы, связанные с парой
            ключей). Идея: сервер и клиент доказывают друг другу, что «свои», без передачи пароля в открытом виде в
            каждом сеансе. У центра (или у сервера) есть цепочка доверия: кто выдал сертификат, кому он выдан, срок
            действия. В панели вы можете видеть выпущенные для пользователей сертификаты, сроки и статусы;{" "}
            <strong>отзыв</strong> сертификата означает, что этим файлом больше нельзя пользоваться для входа.
          </p>

          <h3>Что в этой панели называют «сервером»</h3>
          <p>
            В списке <strong>Серверы</strong> каждая строка — это не абстрактный «VPN в облаке», а{" "}
            <strong>конкретная машина (узел)</strong>, на которой установлен <strong>агент</strong> и, как правило,
            OpenVPN. Панель хранит у себя: как к этому узлу подключаться (протокол, хост, порт), секретный токен и
            отображаемое имя. Всё остальное (запущена ли служба, сколько клиентов) панель узнаёт, опрашивая агент.
          </p>

          <h3>Что такое агент</h3>
          <p>
            <strong>Агент</strong> — служба на Linux, которая слушает HTTP(S)-порт, проверяет токен в запросе и по
            команде панели: отдаёт метрики, читает/пишет разрешённые конфигурационные файлы, показывает список
            VPN-клиентов, может выполнять ограниченный набор команд управления службами и т.д. Без агента панель{" "}
            <strong>не сможет</strong> управлять этим узлом. Подробнее — раздел «Агент и безопасность».
          </p>

          <h3>Зачем нужны «задачи»</h3>
          <p>
            Некоторые операции длятся долго (большой файл, перезапуск цепочки сервисов, применение DNS на узле). Чтобы
            интерфейс не «висел», панель ставит такую работу в <strong>очередь задач</strong>. Вы можете зайти в раздел{" "}
            <strong>Задачи</strong>, увидеть статус (ожидает, выполняется, ошибка, готово) и при сбое —{" "}
            <strong>повторить</strong> после подтверждения.
          </p>
        </>
      );

    case "interface":
      return (
        <>
          <h2>Интерфейс панели</h2>

          <h3>Первый вход</h3>
          <p>
            Откройте в браузере адрес панели, который вам выдали. На экране входа введите <strong>логин и пароль</strong>{" "}
            администратора. После успешного входа вы попадаете в основной интерфейс. Если пароль неверный или сессия
            истекла, панель попросит войти снова.
          </p>

          <h3>Левая колонка с иконками</h3>
          <p>Это главное меню разделов:</p>
          <ul className="doc-list">
            <li>
              <strong>Серверы</strong> — список узлов с агентом и переход в карточку сервера.
            </li>
            <li>
              <strong>Организации</strong> — справочник организаций для привязки пользователей.
            </li>
            <li>
              <strong>Пользователи</strong> — список VPN-пользователей и их профили.
            </li>
            <li>
              <strong>Задачи</strong> — очередь фоновых операций.
            </li>
            <li>
              <strong>Журнал</strong> — просмотр событий и аудита.
            </li>
            <li>
              <strong>Настройки</strong> — администраторы панели, резервное копирование, восстановление.
            </li>
            <li>
              Внизу колонки — <strong>Документация</strong> (текущий раздел).
            </li>
          </ul>

          <h3>Верхняя строка</h3>
          <p>
            <strong>Хлебные крошки</strong> показывают, где вы находитесь. Части пути, которые выглядят как ссылки,
            можно нажать, чтобы подняться на уровень выше (например из карточки сервера — обратно к списку серверов).
            Справа отображаются ваше <strong>имя пользователя</strong>, <strong>оставшееся время сессии</strong> и
            кнопка <strong>Выйти</strong>. Рекомендуется не оставлять открытую панель на общедоступном компьютере без
            блокировки экрана.
          </p>

          <h3>Модальные окна (всплывающие)</h3>
          <p>
            При удалении, остановке службы, повторе задачи и в других случаях поверх страницы открывается окно с текстом
            и кнопками <strong>Отмена</strong> / <strong>Подтвердить</strong>. Пока операция выполняется, кнопки могут
            быть неактивны — не закрывайте вкладку насильно, дождитесь сообщения об ошибке или успехе.
          </p>

          <h3>Прокрутка</h3>
          <p>
            Длинные таблицы и текст прокручиваются в основной области справа; меню слева и верхняя полоса обычно
            остаются на виду, чтобы не терять контекст.
          </p>
        </>
      );

    case "servers":
      return (
        <>
          <h2>Серверы и узлы: с чего начать</h2>

          <h3>Список серверов</h3>
          <p>
            На экране <strong>Серверы VPN</strong> каждая строка — один узел. Вы видите имя, индикатор доступности
            агента (по последнему опросу), адрес вида <code>https://host:9443</code>, загрузку процессора и памяти,
            скорость сети и число активных сессий OpenVPN. Если узел давно не отвечает, метрики могут устареть — это
            сигнал проверить сеть, агент и firewalls.
          </p>

          <h3>Как добавить новый узел</h3>
          <ol className="doc-list" style={{ paddingLeft: "1.25rem" }}>
            <li>
              На сервере Linux установите и настройте <strong>агент</strong> по инструкции вашей организации (порт,
              токен, при необходимости TLS).
            </li>
            <li>
              Убедитесь с панели или другой машины, что порт агента <strong>доступен</strong> по сети (не блокируется
              промежуточным firewall).
            </li>
            <li>
              В панели нажмите добавление сервера, введите <strong>имя</strong> (произвольное удобное название),{" "}
              <strong>протокол</strong> (<code>http</code> или <code>https</code>), <strong>хост</strong> (IP или DNS
              имя), <strong>порт</strong> и <strong>токен</strong> — тот же секрет, что прописан на агенте.
            </li>
            <li>
              Сохраните. Если всё верно, через некоторое время в списке появятся актуальные метрики; если нет — см.
              раздел «Проблемы и советы».
            </li>
          </ol>

          <h3>Что такое токен агента</h3>
          <p>
            Это длинный секрет, известный только панели и агенту. Он передаётся в заголовке запроса. Любой, кто знает
            токен и может достучаться до порта агента, может от имени панели отдавать команды агенту — поэтому токен
            храните как пароль, не вставляйте в публичные тикеты, при утечке смените на агенте и в карточке сервера в
            панели.
          </p>

          <h3>Фильтр по имени</h3>
          <p>
            Поле поиска над таблицей отфильтровывает строки по подстроке в имени сервера — удобно при десятках узлов.
          </p>

          <h3>Переход в карточку</h3>
          <p>
            Нажмите на строку сервера — откроется <strong>карточка узла</strong> с вкладками слева. Подробно все вкладки
            и операции описаны в разделе <strong>«Работа с узлом»</strong>.
          </p>
        </>
      );

    case "working-with-node":
      return (
        <>
          <h2>Работа с узлом (карточка сервера)</h2>
          <p className="doc-lead">
            Карточка — основное место, где вы управляете одной машиной: службой OpenVPN, списком systemd-служб, DNS,
            сессиями, конфигурациями и связью с агентом. Ниже — логика вкладок «с нуля».
          </p>

          <h3>Обзор (служба OpenVPN)</h3>
          <p>
            Здесь обычно показывается, <strong>запущена ли</strong> служба OpenVPN на этом узле, есть ли ошибки опроса,
            параметры management-интерфейса (если используются). Кнопки вроде <strong>Запустить</strong>,{" "}
            <strong>Остановить</strong>, <strong>Перезапустить</strong> отправляют на агент команду управления службой
            OpenVPN (не путать с перезапуском всех служб подряд). Перед выполнением панель покажет{" "}
            <strong>подтверждение</strong> — прочитайте текст: остановка разорвёт все текущие VPN-сессии на этом узле.
          </p>

          <h3>Службы (systemd)</h3>
          <p>
            Linux-сервисы часто управляются утилитой <code>systemctl</code>. Агент получает от панели список интересных
            служб (OpenVPN, dnsmasq, компоненты firewall) и для каждой может показать состояние, PID, время работы с
            момента последнего входа в активное состояние. Таблица на вкладке <strong>Службы</strong> периодически
            обновляется, пока вы на ней находитесь.
          </p>
          <p>
            Ссылки <strong>Запустить</strong> / <strong>Остановить</strong> / <strong>Перезапустить</strong> не показывают
            все три одновременно там, где это бессмысленно: у уже запущенной службы скрыта «Запустить»; у остановленной —
            скрыты «Остановить» и «Перезапустить». Любое действие снова через <strong>модальное подтверждение</strong>.
          </p>
          <p>
            <strong>Важно:</strong> если вы перезапускаете <strong>саму службу агента</strong>, соединение панели с
            узлом может кратковременно оборваться — панель заранее получает ответ «команда принята», а перезапуск
            выполняется чуть позже на сервере, чтобы не было ошибки «разрыв соединения» в браузере.
          </p>

          <h3>DNS (dnsmasq)</h3>
          <p>
            Кратко о смысле вкладки: на многих узлах рядом с OpenVPN используют <strong>dnsmasq</strong> — программу,
            которая отвечает на DNS-запросы клиентов и может подставлять свои ответы (см. отдельный раздел «DNS и
            межсетевой экран»). В панели вы редактируете <strong>представление правил</strong>, которое агент затем
            записывает в конфиг на сервере. Кнопка применения может создать <strong>задачу</strong> в очереди — статус
            смотрите в разделе «Задачи».
          </p>

          <h3>Мониторинг</h3>
          <p>
            Графики и текущие показатели, которые агент собирает с узла. Используйте для оценки нагрузки до и после
            изменений конфигурации или при жалобах пользователей на скорость.
          </p>

          <h3>Активные сессии</h3>
          <p>
            Список подключённых в данный момент VPN-клиентов (по данным OpenVPN на этом узле). При необходимости
            администратор может <strong>принудительно завершить</strong> сессию (например при утерянном устройстве) —
            действие подтверждается и может быть отражено в журнале.
          </p>

          <h3>Сертификаты и ключи на узле</h3>
          <p>
            Здесь отображаются и создаются материалы, связанные с OpenVPN <strong>на этом сервере</strong>: серверные
            сертификаты, DH, tls-auth и т.д. в рамках политики вашей установки. Операции с ключами всегда несут риск
            поломки VPN — выполняйте их по процедуре организации и с пониманием последствий.
          </p>

          <h3>Конфигурация агента</h3>
          <p>
            Поля, которые описывают, <strong>как панель подключается к агенту</strong>: имя записи, протокол, хост,
            порт. Поле токена: если оставить пустым при сохранении, токен на узле в панели <strong>не меняется</strong>;
            если ввести новый — он должен совпасть с тем, что вы настроили на агенте. Кнопка <strong>Сохранить</strong>{" "}
            обновляет учётные данные узла в базе панели.
          </p>
          <p>
            Кнопка <strong>Обновить</strong> открывает окно для загрузки <strong>нового бинарного файла агента</strong>:
            вы выбираете файл на своём компьютере, панель отправляет его на сервер (процесс может занять время), агент
            заменяет исполняемый файл и перезапускает свою службу. В окне отображается ход отправки и фрагмент журнала
            службы до перезапуска. После обновления проверьте, что узел снова появляется в списке с актуальными данными.
          </p>

          <h3>Конфигурация службы OpenVPN и клиента</h3>
          <p>
            Формы для правки настроек сервера и клиентского профиля (в терминах вашей установки). Часто есть версии
            конфигурации, черновики и кнопки «сохранить на панели» / «применить на узел». Долгое применение отражается в{" "}
            <strong>Задачах</strong>. Перед массовым изменением делайте <strong>резервную копию</strong> панели и
            убедитесь, что есть план отката на стороне сервера.
          </p>

          <h3>Межсетевой экран на узле</h3>
          <p>
            Вкладка для правил firewall <strong>на этом сервере</strong> (iptables/nftables и т.д. в той модели, которую
            поддерживает ваш агент). Смысл — разрешить или запретить трафик VPN и связанные цепочки. Подробнее см. раздел
            «DNS и межсетевой экран».
          </p>

          <h3>Журнал OpenVPN на сервере</h3>
          <p>
            Просмотр событий, которые агент может прочитать с узла (файлы логов OpenVPN). Полезно при диагностике: не
            подключается клиент, отбрасывается сертификат, ошибка в конфиге.
          </p>
        </>
      );

    case "dns-firewall":
      return (
        <>
          <h2>DNS (dnsmasq) и межсетевой экран для начинающего администратора</h2>

          <h3>Зачем клиентам VPN нужен DNS</h3>
          <p>
            Когда пользователь в VPN открывает в браузере имя вроде <code>internal.company.local</code>, компьютер
            спрашивает у DNS-сервера: «какой у этого имени IP-адрес?». В корпоративной сети часто есть <strong>внутренние
            имена</strong>, которые не видны из интернета. Через VPN клиент должен использовать DNS, который знает эти
            имена — иначе сайты по коротким именам не откроются, даже если туннель установлен.
          </p>

          <h3>Что такое dnsmasq одной фразой</h3>
          <p>
            <strong>dnsmasq</strong> — это небольшая программа на Linux, которая часто выполняет роль <strong>кэширующего
            DNS</strong> и может отдавать свои записи (вперёд на другие серверы или по локальным правилам). В контексте
            OpenVPN её нередко ставят на тот же сервер, что и VPN, чтобы клиентам выдавать нужные DNS-серверы и списки
            имён. <strong>Вам не обязательно уметь настраивать dnsmasq вручную в консоли</strong>, если ваша организация
            использует вкладку DNS в панели — но полезно понимать: вы редактируете <strong>логику резолвинга для
            клиентов</strong>, а не «просто текст в файле без последствий».
          </p>

          <h3>Что вы делаете на вкладке DNS в панели</h3>
          <ul className="doc-list">
            <li>
              Видите (или редактируете) набор <strong>правил</strong>: домены, перенаправления, параметры кэша и т.п. в
              форме, которую предоставляет панель.
            </li>
            <li>
              Сохраняете черновик и применяете на узел — агент записывает согласованный конфиг в файл на сервере и при
              необходимости перезапускает <code>dnsmasq</code> (иногда через задачу в очереди).
            </li>
            <li>
              Если что-то пошло не так, в <strong>Задачах</strong> появится ошибка; клиенты могут временно потерять
              корректный DNS — имейте план отката (копия старого конфига, резервная копия панели).
            </li>
          </ul>

          <h3>Межсетевой экран (firewall) простыми словами</h3>
          <p>
            <strong>Межсетевой экран</strong> — правила на уровне операционной системы (или сетевого оборудования), которые
            говорят: «этот пакет разрешить», «этот отбросить». Для VPN важно: разрешён ли вход на порт OpenVPN с
            интернета или с корпоративной сети, разрешён ли форвардинг между клиентами VPN и внутренними подсетями,
            нужны ли отдельные правила NAT.
          </p>
          <p>
            В Linux это часто реализовано через <strong>iptables</strong> или <strong>nftables</strong>; в панели вы
            можете видеть вкладки firewall на <strong>сервере</strong>, у <strong>организации</strong> или у{" "}
            <strong>пользователя</strong> — это <strong>разные уровни</strong> политики, которые в итоге агент может
            сливать в runtime-скрипты на узле. Начинающему администратору важно: <strong>ошибка в правилах может отрезать
            доступ</strong> к серверу или к внутренним сетям — меняйте по одному логическому блоку, проверяйте, держите
            консольный доступ на случай блокировки.
          </p>

          <h3>Уровни правил в панели (логика)</h3>
          <ul className="doc-list">
            <li>
              <strong>Сервер</strong> — общие правила на конкретном узле (например базовая защита VPN-шлюза).
            </li>
            <li>
              <strong>Организация</strong> — дополнительные ограничения или разрешения для всех пользователей этой
              организации.
            </li>
            <li>
              <strong>Пользователь</strong> — индивидуальные исключения (например отдельный доступ для одного
              сотрудника).
            </li>
          </ul>
          <p>
            Точный порядок применения и приоритет зависят от реализации агента и шаблонов вашей организации — при
            сомнениях согласуйте с тем, кто внедрял панель.
          </p>

          <div className="doc-callout">
            Если вы не уверены в правиле, сначала опишите словами желаемый результат («клиенты VPN должны видеть только
            подсеть X»), затем найдите в интерфейсе соответствующий тип правила или обратитесь к коллеге. Неправильный
            NAT или DROP может выглядеть как «VPN подключился, но ничего не пингуется».
          </div>
        </>
      );

    case "users-organizations":
      return (
        <>
          <h2>Пользователи и организации</h2>

          <h3>Организации</h3>
          <p>
            <strong>Организация</strong> — запись-справочник: название, реквизиты, контакты. Пользователи VPN могут быть
            к ней привязаны для отчётности и для применения общих политик (например межсетевого экрана на уровне
            организации). Создание и редактирование доступно из раздела <strong>Организации</strong>.
          </p>

          <h3>Пользователи VPN</h3>
          <p>
            В разделе <strong>Пользователи</strong> — таблица сотрудников (или учётных записей VPN): ФИО, должность,
            организация, контакты. Есть поиск по нескольким полям. Кнопка добавления открывает форму нового пользователя.
          </p>

          <h3>Карточка пользователя</h3>
          <p>
            По клику на строку открывается профиль с <strong>вкладками</strong>. Типичный набор:
          </p>
          <ul className="doc-list">
            <li>
              <strong>Обзор</strong> — основные поля профиля, организация, примечания.
            </li>
            <li>
              <strong>Сессии</strong> — история и текущие подключения.
            </li>
            <li>
              <strong>Сертификаты</strong> — выпущенные для этого пользователя файлы, сроки, отзыв.
            </li>
            <li>
              <strong>CCD / параметры туннеля</strong> — персональные маршруты, push-настройки (в терминах OpenVPN).
            </li>
            <li>
              <strong>Межсетевой экран</strong> — правила уровня пользователя.
            </li>
          </ul>
          <p>
            Операции вроде <strong>отзыва сертификата</strong> или отвязки обычно требуют подтверждения: пользователь
            перестанет подключаться старым профилем до выдачи нового.
          </p>

          <h3>Рекомендации по процессу</h3>
          <ul className="doc-list">
            <li>Ведите актуальные контакты — при компрометации сертификата быстрее связаться с человеком.</li>
            <li>
              Увольнение: отзовите сертификаты, при необходимости завершите сессии на серверах, проверьте firewall.
            </li>
            <li>Новый сотрудник: создайте пользователя, выпустите сертификат по процедуре, выдайте профиль подключения.</li>
          </ul>
        </>
      );

    case "logs-tasks":
      return (
        <>
          <h2>Журнал и задачи</h2>

          <h3>Журнал</h3>
          <p>
            Раздел <strong>Журнал</strong> содержит подразделы (в левом меню внутри раздела):
          </p>
          <ul className="doc-list">
            <li>
              <strong>Назначение VPN-IP</strong> — какие внутренние адреса когда-либо выдавались клиентам (удобно для
              аудита и поиска конфликтов).
            </li>
            <li>
              <strong>Исходные IP</strong> — с каких внешних адресов подключались клиенты.
            </li>
            <li>
              <strong>Действия администраторов</strong> — кто из админов панели что менял (если эта подсистема включена в
              вашей сборке).
            </li>
          </ul>
          <p>
            Используйте поиск и пагинацию; данные хранятся на стороне <strong>панели</strong>, а не на каждом VPN-сервере
            отдельно.
          </p>

          <h3>Задачи</h3>
          <p>
            Когда панель запускает долгую операцию (применить большой конфиг, обновить DNS на узле и т.д.), она создаёт
            запись в <strong>Задачах</strong>. В таблице видно:
          </p>
          <ul className="doc-list">
            <li>
              <strong>Тип</strong> — внутреннее имя операции (ориентир для поддержки).
            </li>
            <li>
              <strong>Статус</strong> — ожидание, выполнение, успех, ошибка.
            </li>
            <li>
              <strong>Узел</strong> — какой сервер затронут (если применимо).
            </li>
            <li>
              <strong>Ошибка</strong> — текст от агента или панели для диагностики.
            </li>
          </ul>
          <p>
            Ссылка <strong>Повторить</strong> (не для задач «в процессе») открывает подтверждение и ставит задачу в
            очередь снова — используйте после устранения причины сбоя (сеть, права агента, синтаксис конфига).
          </p>
        </>
      );

    case "panel-settings":
      return (
        <>
          <h2>Настройки панели</h2>

          <h3>Администраторы</h3>
          <p>
            Здесь перечислены учётные записи, которые могут входить в веб-интерфейс. Статусы в списке могут отображаться
            цветом: активен, ожидает перехода по приглашению, заблокирован. Доступны действия: создать приглашение,
            сбросить пароль (ссылка одноразовая), заблокировать доступ. Любое деструктивное действие сопровождается
            проверками в интерфейсе.
          </p>

          <h3>Резервное копирование и восстановление</h3>
          <p>
            Панель может создавать <strong>архивы состояния</strong> (файлы конфигурации и данных приложения по политике
            вашей установки). Настройте расписание и хранение в разделе резервного копирования. Восстановление — загрузка
            архива и подтверждённый откат; операция критична: выполняйте в окно обслуживания и убедитесь, что есть копия
            «на всякий случай» вне панели.
          </p>

          <div className="doc-callout">
            Резервная копия панели не заменяет резервное копирование самих VPN-серверов и секретов на узлах. Держите
            отдельный план восстановления для критичных машин.
          </div>
        </>
      );

    case "agent-security":
      return (
        <>
          <h2>Агент и безопасность</h2>

          <h3>Роль агента</h3>
          <p>
            Агент — «руки» панели на сервере. Он не должен быть доступен из интернета без необходимости. Минимизируйте
            открытые порты: по возможности VPN-администратор подключается к панели через VPN или с административной сети,
            а панель ходит к агентам по внутренним маршрутам.
          </p>

          <h3>Токен и TLS</h3>
          <ul className="doc-list">
            <li>
              Используйте <strong>HTTPS</strong> для агента в продакшене, если это поддерживает ваша схема установки.
            </li>
            <li>
              Токен храните в менеджере секретов; при увольнении админа, имевшего доступ к токенам узлов, ротируйте токены.
            </li>
            <li>
              Не давайте агенту избыточных прав в ОС: достаточно того, что требуется для объявленных операций (конкретные
              каталоги конфигов, systemctl для перечисленных unit’ов).
            </li>
          </ul>

          <h3>Обновление агента</h3>
          <p>
            Выполняйте из карточки сервера только проверенными сборками. После обновления проверьте версию в отчётах
            панели и работоспособность опроса узла.
          </p>
        </>
      );

    case "troubleshooting":
      return (
        <>
          <h2>Проблемы и практические советы</h2>

          <h3>Сервер в списке «не оживает» после добавления</h3>
          <ul className="doc-list">
            <li>Проверьте с машины панели: <code>ping</code> до хоста и доступность порта (telnet/nc/curl).</li>
            <li>Убедитесь, что токен в панели совпадает с токеном на агенте символ в символ.</li>
            <li>
              Проверьте firewall <strong>на пути</strong> панель → узел и на самом узле (INPUT на порт агента).
            </li>
            <li>
              Если используете HTTPS на агенте, убедитесь, что панель доверяет сертификату или используйте корректный
              режим в настройках.
            </li>
          </ul>

          <h3>VPN клиент подключается, но нет доступа к внутренним именам</h3>
          <ul className="doc-list">
            <li>
              Проверьте, какие DNS-серверы получает клиент (в профиле OpenVPN / push от сервера). Сверьте с вкладкой DNS
              (dnsmasq) на сервере.
            </li>
            <li>
              Убедитесь, что dnsmasq запущен и без ошибок в логах на узле.
            </li>
          </ul>

          <h3>«Всё пинговалось, после правки firewall — нет»</h3>
          <p>
            Откатите последнее изменение через панель (если доступна форма), восстановите из резервной копии или
            подключитесь к серверу по out-of-band (IPMI, консоль облака) и исправьте правила вручную. Именно поэтому
            важно менять firewall поэтапно.
          </p>

          <h3>Задача завершилась с ошибкой</h3>
          <p>
            Прочитайте текст ошибки в таблице задач; часто там путь к файлу или вывод команды на узле. Исправьте причину
            и используйте <strong>Повторить</strong>.
          </p>

          <h3>Сессия панели внезапно сбрасывается</h3>
          <p>
            Истёк срок JWT/сессии — войдите снова. При частых сбросах проверьте время на серверах панели и браузере (NTP).
          </p>

          <div className="doc-callout">
            Если после прочтения документации остаётся неопределённость по <strong>вашей</strong> схеме сети, согласуйте с
            внедренцем: одна и та же кнопка в интерфейсе может соответствовать разным политикам организации.
          </div>
        </>
      );

    default:
      return (
        <p className="muted">
          Раздел не найден. Выберите пункт в оглавлении слева или{" "}
          <button type="button" className="app-link-btn" onClick={() => navigate(paths.documentation())}>
            перейти ко введению
          </button>
          .
        </p>
      );
  }
}

/**
 * Пользовательская документация: оглавление в стиле resource-sidebar и HTML-разметка разделов.
 */
interface DocumentationPageProps {
  section: string;
  navigate: NavigateFunction;
}

export function DocumentationPage({ section, navigate }: DocumentationPageProps) {
  return (
    <div className="resource-layout">
      <aside className="resource-sidebar" aria-label="Оглавление документации">
        <div className="resource-sidebar-header">
          <h2 className="resource-sidebar-title">Документация</h2>
          <p className="resource-sidebar-sub">OVPN Control</p>
        </div>
        <nav className="resource-nav" aria-label="Разделы руководства">
          {SECTIONS.map((s) => (
            <button
              key={s.id}
              type="button"
              className={`resource-nav-item${section === s.id ? " is-active" : ""}`}
              aria-current={section === s.id ? "page" : undefined}
              onClick={() => navigate(paths.documentation(s.id))}
            >
              {s.label}
            </button>
          ))}
        </nav>
      </aside>
      <div className="resource-main doc-resource-main">
        <article className="doc-html-content" lang="ru">
          <DocBody section={section} navigate={navigate} />
        </article>
      </div>
    </div>
  );
}
