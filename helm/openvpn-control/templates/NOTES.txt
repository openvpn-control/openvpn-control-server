OpenVPN Control has been deployed.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Frontend service:
  {{ include "openvpn-control.fullname" . }}-frontend:{{ .Values.frontend.service.port }}

Backend service:
  {{ include "openvpn-control.fullname" . }}-backend:{{ .Values.backend.service.port }}

Postgres service:
  {{ include "openvpn-control.fullname" . }}-postgres:{{ .Values.postgres.service.port }}

If ingress is disabled, you can access frontend with port-forward:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "openvpn-control.fullname" . }}-frontend 8081:{{ .Values.frontend.service.port }}

Then open:
  http://127.0.0.1:8081
