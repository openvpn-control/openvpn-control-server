apiVersion: v1
kind: Service
metadata:
  name: {{ include "openvpn-control.fullname" . }}-backend
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    app: {{ include "openvpn-control.fullname" . }}-backend
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 8080
