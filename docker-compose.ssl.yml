# Edge nginx с certbot (Let's Encrypt). Использование:
#   docker compose -f docker-compose.yml -f docker-compose.ssl.yml up --build

services:
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile.certbot
